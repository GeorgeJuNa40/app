# Configuración de Netlify para Move yA (app Vite + React SPA).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Cualquier ruta sirve index.html (app de una sola página).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
